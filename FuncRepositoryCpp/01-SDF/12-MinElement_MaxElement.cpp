#include <bits/stdc++.h>
using namespace std;
#define all(a) (a).begin(),(a).end()

int main() {
    vector<int> v = {3,2,7,1,8};

    int Min = *min_element(all(v));

    int Max = *max_element(all(v));
}